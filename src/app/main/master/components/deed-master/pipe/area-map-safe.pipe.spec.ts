import { AreaMapSafePipe } from './area-map-safe.pipe';

describe('AreaMapSafePipe', () => {
  it('create an instance', () => {
    const pipe = new AreaMapSafePipe();
    expect(pipe).toBeTruthy();
  });
});
